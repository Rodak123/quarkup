# Single project + style demo

[Back](../../README.md)

Command: `s ./main.md -o ./test-web/html`

Converts the source markdown file into html and saves it into the
output folder as `<output>/index.html`